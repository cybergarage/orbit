# Specify before implementing

Review `spec.md` and identify missing implementation details, contradictions, and conditions that are difficult to test.

Do not change the code or `spec.md` at this point. Present the problems you found and the content that should be added to or corrected in `spec.md`. Do not search outside the working directory.

---

After reviewing and updating the specification, use the following instruction.

Implement the memory matching game according to `spec.md`.

Do not add new dependencies or access anything outside the working directory. Use only `npm run typecheck`, `npm test`, and `npm run build` for verification. For each completion criterion in the specification, report the verification method and result. Do not perform browser checks; report the items that a person must verify.
